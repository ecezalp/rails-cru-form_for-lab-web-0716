<h1> Song: <%= link_to @song.name, song_path(@song) %>  </h1> 
<h1> Genre: <%= link_to @song.genre.name, genre_path(@song.genre_id) %> </h1>
<h1> Artist: <%= link_to @song.artist.name, artist_path(@song.artist_id) %> </h1>

